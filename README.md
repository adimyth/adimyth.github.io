# adimyth.in

Personal site. Essays live at `/`. About lives at `/about`.

```bash
npm install
npm run dev
```

Edit copy in `lib/data.ts`. Essays are MDX files in `content/essays`. Push to `main` to deploy on GitHub Pages.
